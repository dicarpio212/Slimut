
import { User } from './types';

// NOTE: The hardcoded user data has been removed.
// All user data is now managed in your Appwrite database.
// You can create initial users (like an admin) directly in the Appwrite console.
// New users can register through the application's registration form.

export const USERS_DATA: User[] = [];
